#include <stdio.h>
#include <math.h>
#include <float.h>
int main(){
	double n,r,s;
	int i;
	n = pow(2, 1000); 
	s = 0;
	r = 0;
	//while(n != 0){
	//	r = fmod(n,10);
	//	s = s + r;
	//	n = floor(n / 10);
    // }
	printf("%lf\n",  DBL_MAX);

	return 0;
}